# Runtime 错误信息整改建议

## 范围

- `<待整改目录或文件路径>`
- `<待整改目录或文件路径>`

## 拟整改项

说明：表中“保持返回码不变”是指不改变原有控制流和业务返回码，仅调整结构化错误上报宏、错误码或错误文案。

| 位置 | 当前问题 | 分类判断 | 当前错误码 | 拟整改错误码 | 拟整改方案 | 拟整改错误文案 |
| --- | --- | --- | --- | --- | --- | --- |
| `<文件路径>:<行号>` | `<描述当前错误上报、错误码、宏或文案问题>` | `<用户错误/内部错误/防御性编程/重复上报/普通日志文案优化>` | `<当前错误码或无结构化错误码>` | `<拟使用错误码或无结构化错误码>` | `<说明拟替换的宏、参数、文案；明确保持返回码和控制流不变>` | `<拟整改后的完整错误文案>` |

## 建议保持不改项

| 位置 | 保持不改原因 |
| --- | --- |
| `<文件路径>:<行号>` | `<说明属于普通日志、防御性编程、下层已上报、driver 调用边界或业务逻辑不应变更>` |

## 不确定需确认项

| 位置 | 不确定点 | 需确认信息 |
| --- | --- | --- |
| `<文件路径>:<行号>` | `<说明当前无法确认的问题>` | `<需要业务、调用链、返回码语义或错误码归属确认的信息>` |
